name: provider-v7-go
runtime: yaml
plugins:
  providers:
    - name: v7-go
      path: ../../bin

resources:
  myProject:
    type: v7-go:Project
    # properties:
    #   length: 24

outputs:
  output:
    value: ${myProject.result}
