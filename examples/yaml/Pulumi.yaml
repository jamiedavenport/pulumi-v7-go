name: provider-v7-go-native
runtime: yaml
plugins:
  providers:
    - name: v7-go
      path: ../../bin

resources:
  myRandomResource:
    type: v7-go:Random
    properties:
      length: 24

outputs:
  output:
    value: ${myRandomResource.result}
